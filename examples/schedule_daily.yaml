# Create a single volume monitor for runs table running daily with time partitioning shift.

monitors:
  - id: runs_volume_daily
    type: volume
    time_partitioning: created_at
    timezone: Europe/Prague
    daily:
      query_delay: 1h
    monitored_ids:
      - ch-prod.default.runs
