# yaml-language-server: $schema=../../schema.json
# Create a single volume monitor for runs table with defaults (schedule=daily, severity=Error)

monitors:
  - id: runs_volume_daily
    name: "Runs volume daily" # human-readable monitor name
    type: volume
    time_partitioning: created_at
    monitored_ids:
      - ch-prod.default.runs
